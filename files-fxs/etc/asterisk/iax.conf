
; Inter-Asterisk eXchange driver definition
;
; This configuration is re-read at reload
; or with the CLI command
; 	reload chan_iax2.so
;
; General settings, like port number to bind to, and
; an option address (the default is to bind to all
; local addresses).
;
[general]

bandwidth=low
;
; You can also fine tune codecs here using "allow" and "disallow" clauses
; with specific codecs.  Use "all" to represent all formats.
;
;allow=all			; same as bandwidth=high
disallow=lpc10			; Icky sound quality...  Mr. Roboto.

jitterbuffer=no
forcejitterbuffer=no

;register => marko:moofoo@10.130.2.222


autokill=yes


; External IAX2 host and clients support include file
; ----------------------------------------------------
; External IAX2 host support include file
; Caution: This include file is auto generated by Dragino Web Interface
; You may comment out this #include line if using custom settings file #include below to avoid conflicts
#include "dragino.iax2.conf"





;[marko]
;type=friend
;host=dynamic
;regexten=1234
;secret=moofoo   ; Multiple secrets may be specified. For a "user", all
;secret=foomoo   ; specified entries will be accepted as valid. For a "peer",
;secret=shazbot  ; only the last specified secret will be used.
;context=default
;permit=0.0.0.0/0.0.0.0


